pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "Wasim"
//includeBuild("buildsystem")

include ':app'
include ':core'
include 'modules:feature-testrun'
include 'modules:feature-testrun-abstract'